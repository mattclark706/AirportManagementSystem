using System.Text.Json;

namespace AirportManagementSystem.Utilities
{
    public class FlightDataLoader
    {
        /*public static List<Models.Flight> LoadFlights(string filepath)
        {
            var json = File.ReadAllText(filepath);
            var flights = JsonSerializer.Deserialize<List<Models.Flight>>(json);
            return flights ?? new List<Models.Flight>();
        }*/
    }
}
